# fixtures

One folder per source. Real exports committed verbatim; expected outputs as
sibling JSON files. See E1.9 in the backlog.

Example layout once populated:

```
fixtures/
├── decklists/
│   ├── missing-set-code.txt
│   ├── foil-marker.txt
│   └── split-card.txt
├── melee/
│   ├── event-123.csv
│   └── event-123.expected.json
└── challonge/
    └── ...
```
