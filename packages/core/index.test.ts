import { describe, expect, it } from "vitest";

// Placeholder so CI is green from the first commit. Delete this once real
// modules exist under this package — each module gets its own index.test.ts
// (see §6 of the master plan and E1.8's module generator).
describe("@ps/core", () => {
  it("has no modules yet", () => {
    expect(true).toBe(true);
  });
});
