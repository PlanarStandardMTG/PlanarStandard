node_modules/
dist/
.next/
.turbo/
*.tsbuildinfo
.env
.env.local
.DS_Store
coverage/
.supabase/
supabase/

# Generated by Next on every build, and it references .next/, which is ignored.
apps/web/next-env.d.ts

# Playwright MCP scratch output.
.playwright-mcp/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Scryfall bulk downloads. The pruned artifact in data/cards/ is what we commit;
# the source file is ~75MB gzipped and must never reach a commit.
data/cards/*.jsonl
data/cards/*.jsonl.gz
